Algoritmo "temperatura"

Var

   cel , far : real

Inicio
        escreva( " a tabela de conversão é = " )
        escreva( " digite a temperatura em celsius = " )
        leia(cel)
        far <- (9*cel+160)/5
        escreval("A nova temperatura é: ",far,"ºF")


Fimalgoritmo