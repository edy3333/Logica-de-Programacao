Algoritmo "semnome"

Var
        nota1 , nota2 , nota3 : inteiro
        mediaFinal : real


Inicio
    escreva( " prova 1 ? " )
    leia(nota1)
    escreva( " prova 2 ? " )
    leia(nota2)
    escreva( " prova 3 ? " )
    leia(nota3)
    mediaFinal <- nota1 + nota2 + nota3 / 3
    escreva( " a media final é : " , mediaFinal)
    
    
        

Fimalgoritmo